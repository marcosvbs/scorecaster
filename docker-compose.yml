services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=True
      - SECRET_KEY=local-dev-secret-key
    command: python manage.py runserver 0.0.0.0:8000